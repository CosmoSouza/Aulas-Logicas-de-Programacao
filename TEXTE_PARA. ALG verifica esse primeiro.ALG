Algoritmo "texte_para"

Var

 n, i, x, soma : inteiro

Inicio

    escreva("Quantos numeros serao digitados? ")
     leia(N)
     
     soma <- 0

     para i de 1 ate N faca

       escreva("Digite um numero: ")
       leia(X)
       soma <- soma + x
       fimpara
       
       escreval("soma = ", soma)


Fimalgoritmo